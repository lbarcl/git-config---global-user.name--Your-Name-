# Server settings
server:
  server-ip: "0.0.0.0"
  server-port: 25565
  online-mode: true
  enable-status: true
  log-ips: true
  rate-limit: 0
  # enable-compression: true
  # network-copmression-limit: 60

# Miscellaneous
misc:
  motd: "A Minecraft Server"

# Game settings
game:
  max-players: 20